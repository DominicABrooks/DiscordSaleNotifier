import React from "react";
import DeleteTrackingForm from "./DeleteTrackingForm";

const meta = {
  title: "Components/DeleteTrackingForm",
  component: DeleteTrackingForm
};

export default meta;

export function Default() {
  return React.createElement(DeleteTrackingForm);
}
