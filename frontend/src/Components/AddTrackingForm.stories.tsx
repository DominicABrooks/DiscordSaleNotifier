import React from "react";
import AddTrackingForm from "./AddTrackingForm";

const meta = {
  title: "Components/AddTrackingForm",
  component: AddTrackingForm
};

export default meta;

export function Default() {
  return React.createElement(AddTrackingForm);
}
