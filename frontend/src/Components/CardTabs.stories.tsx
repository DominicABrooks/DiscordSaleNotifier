import React from "react";
import CardTabs from "./CardTabs";

const meta = {
  title: "Components/CardTabs",
  component: CardTabs
};

export default meta;

export function Default() {
  return React.createElement(CardTabs);
}
